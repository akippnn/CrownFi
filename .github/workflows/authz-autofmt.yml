name: One-shot authorization rustfmt

on:
  push:
    branches:
      - fix/authorization-acl-v1
    paths:
      - services/api/src/authorization_acl.rs
      - .github/workflows/authz-autofmt.yml

permissions:
  contents: write

jobs:
  rustfmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: fix/authorization-acl-v1
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Apply rustfmt
        run: cargo fmt --manifest-path services/api/Cargo.toml
      - name: Commit formatting when needed
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add services/api/src/authorization_acl.rs services/api/src/main.rs
          git commit -m "style(authz): apply rustfmt"
          git push origin HEAD:fix/authorization-acl-v1
